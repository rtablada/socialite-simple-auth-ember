<h2 id='title'>Ember Simple Auth &amp; Torii &amp; Laravel Socialite</h2>

{{#if session.isAuthenticated}}
  <button {{ action 'invalidateSession' }}>Logout</button>
{{else}}
  <button {{action 'signInViaGithub'}}>
    Login With Github
  </button>
{{/if}}

{{outlet}}
